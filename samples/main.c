#include <stdlib.h>
#include "paranoid_backup.h"
#include "paranoid_backup_restore.h"

int main(int argc, char** argv)
{
    // pb_init();
    // pb_init_from_config("./paranoid_backup.config");

    // pb_get_storage_endpoints();
    // pb_add_storage_endpoint();
    // pb_remove_storage_endpoint();
    // pb_update_storage_endpoint();

    // pb_start_storage_endpoint(1234);
    // pb_stop_storage_endpoint(1234);

    // pb_get_storages();
    // pb_add_storage();
    // pb_remove_storage();
    // pb_update_storage();

    // pb_get_targets();
    // pb_add_target();
    // pb_remove_target();
    // pb_update_target();

    // pb_get_routines();

    // pb_get_versions_by_path();

    // PB_RESTORE_REQUEST request;
    // request.version_id = versions[0]->id;
    // request.source_path = "/";
    // request.destination_path = "/";
    // request.on_progress_report_callback = NULL;
    // request.on_progress_report_context = NULL;

    // if (pb_restore_async(&request) != 0)
    // {

    // }
    // else
    // {

    // }

    // pb_deinit();

    return 0;
}